## ✅ P0：已完成（2026-08-06）

- ~~T1.1 `review_reputation` 路由不可达~~：已补 `@app.post` 装饰器，`test_talent_pool.py` 两条测试转绿。
- ~~T1.2 `research_group_matching` 死字段~~：研究组匹配功能整体下线（JD 持续变动，不再维护），前后端字段/界面/测试已全部移除。
- ~~T1.3 过期 docstring~~：`services/__init__.py`、`talent_service.py` docstring 已更新为实际状态。

---

## 🔥 P1：连孤岛（这是你说的"连贯性"问题的核心解药）

### T2. 问答引用角标可跳转候选人档案 ✅（2026-08-06 已完成）

- **现状**：引用卡片已升级为详细档案卡（姓名/机构/方向/分组/教育/最新评估），带"人才库定位"（`/talent-pool?focus=<id>`）和"完整档案"（`/talent-pool/<id>`）双按钮；所有人物相关工具（search_persons/get_person_profile/get_person_evaluation/get_resume_versions）的引用都携带 person meta。

### T3. 候选人档案页可发起"基于此人的问答"

- **现状**：`TalentProfile.tsx` / `TalentDetail.tsx` 完全没有"发起问答"入口
- **改法**：档案页加一个按钮"问问 AI 这个人"，预填 prompt（如"对比 {name} 和库里类似背景的人"），跳到 `/` 问答页并发起会话
- **代价**：半天（前端 + 后端加一个 `POST /api/conversations` 带 `person_id` 字段）
- **价值**：双向打通。会话挂到候选人档案下，知识沉淀

### T4. 评估页 ↔ 人才库页统一跳转

- **现状**：评估页用 `CandidateBrief`，人才库用 `PersonBrief`，两套数据模型，页面之间无跳转
- **改法**：评估列表点候选人 → 跳 `/talent-pool/:personId`（如果有 person_id）；人才库点 person → 可以"查看评估过程"跳回评估页
- **代价**：1 天（主要是数据模型对齐）
- **价值**：消除 HR 心智撕裂，"刚评估完的人为什么换页面就找不到了"

### T5. 候选人状态历史 UI 化

- **现状**：后端 `engagementHistory` API 已实现（`api.ts:78`），但前端无 UI
- **改法**：在 TalentProfile 详情页加一个"流转历史"时间轴组件，展示从 `newly_admitted → screening → interviewing` 的所有变更 + `changed_by` + `note`
- **代价**：半天
- **价值**：HR 可以回溯"为什么这个人从面试中变成已淘汰"，**审计性大幅提升**

---

## 🎯 P2：补短板（1-2 周，提升产品完整度）

### T6. 评估报告一键导出/分享

- **现状**：完全无分享能力（无 PDF、无链接、无复制）
- **最小方案**：
  - **第一步（1 天）**：报告页加"复制 Markdown"按钮，HR 能粘到飞书/钉钉
  - **第二步（3 天）**：生成**可分享的报告链接**（带 token + 有效期），后端加 `GET /api/share/evaluations/:id?token=xxx`，前端独立只读页
- **价值**：**这是 AI 评估从"HR 私人工具"变成"说服业务方武器"的关键**。字节 COE 极可能问"评估结果怎么给业务面"

### T7. 简单的管道看板（Kanban）

- **现状**：`engagement_status` 有 12 个枚举，但只有平铺按钮，无看板视图
- **改法**：在 TalentPool 页加一个"看板视图"切换，列 = 状态（待初筛 / 面试中 / Offer / 已入职），卡片 = person，支持拖拽改状态
- **代价**：2-3 天（前端为主，用 `@dnd-kit`，项目里已装）
- **价值**：把"找→评→推→面"的进度可视化，是招聘系统的标配

### T8. 手动加入的人也能评估（消除"二等公民"）

- **现状**：`guest` 类型不参与 Track 分类，批量评估时整列表被禁用（`TalentList.tsx:420`）
- **改法**：放宽限制——guest 也能跑通用潜力评估（不跑 Track 也行）；或批量评估时**自动跳过 guest** 而不是禁用整个操作
- **代价**：1 天
- **价值**：消除一个反直觉的产品规则

### T9. 基础数据看板（Dashboard）

- **现状**：统计只能通过聊天问答触发（`aggregate_persons`），无独立看板
- **最小方案**：加一个 `/dashboard` 页，3 个卡片：
  - 本月评估人数 + S/A/B/C 分布
  - 各 Track 人才库规模（柱状图）
  - 候选人状态漏斗（newly_admitted → interviewing → hired）
- **代价**：2 天（前端 + 1 个聚合 API）
- **价值**：PM/招聘负责人要看的数据沉淀价值

---

## 🌟 P3：锦上添花（有空再做）

### T10. 筛选条件持久化（命名视图）

- **现状**：筛选只存 sessionStorage，不能保存"清华 + AI Infra track"这种常用组合
- **改法**：加"保存当前筛选"按钮，存到后端，下次一键加载
- **代价**：1 天

### T11. 评论/备注流（轻量协作）

- **现状**：候选人只有论文核验备注和"补充信息"自由文本，无评论流
- **改法**：在 TalentProfile 加评论区组件，支持多人评论（先不做 @ 和通知）
- **代价**：2-3 天（含后端 CommentORM）
- **价值**：招聘是协作决策，这是通向"团队工具"的第一步

### T12. 简历导入来源追踪

- **现状**：只记 `source_format`（pdf/text），不记招聘渠道
- **改法**：上传时让 HR 选/填"来源"（内推/猎头/校招/主动投递/外部调查），存到 `CandidateORM.sources`
- **代价**：半天
- **价值**：未来能回答"哪个渠道 ROI 最高"

### T13. 评估报告内嵌"复制面谈问题"

- **现状**：`interview_questions` 是 LLM 生成的建议题，HR 看到但没法直接用
- **改法**：每条建议题旁边加"复制"按钮 + "加入面谈清单"
- **代价**：半天
- **价值**：让 AI 的输出**真正落到下一步动作**上

### T14. 问答会话挂候选人（修断点 5）

- **现状**：会话 `ChatConversation` 独立，无法挂到 person
- **改法**：`ChatConversation` 加 `person_id` 字段（可空），档案页能"查看关于此人的所有问答"
- **代价**：1 天
- **价值**：知识沉淀闭环

---

## 📋 一张表汇总（按工作量+收益排）

| 优先级 | TODO                 | 类型 | 工作量      | 收益               |
| ------ | -------------------- | ---- | ----------- | ------------------ |
| ~~P0~~ | ~~T1 修 3 个半成品 bug~~ | 工程 | ✅ 已完成    | ⭐⭐⭐⭐⭐ 防 demo 翻车 |
| ~~P1~~ | ~~T2 问答引用可跳档案~~  | 前端 | ✅ 已完成    | ⭐⭐⭐⭐⭐ 连贯性核心   |
| P1     | T3 档案页发起问答    | 全栈 | 半天        | ⭐⭐⭐⭐⭐ 连贯性核心   |
| P1     | T4 评估↔人才库跳转   | 前端 | 1 天        | ⭐⭐⭐⭐               |
| P1     | T5 状态历史 UI       | 前端 | 半天        | ⭐⭐⭐⭐               |
| P2     | T6 报告分享/导出     | 全栈 | 1-4 天      | ⭐⭐⭐⭐⭐ 业务侧闭环   |
| P2     | T7 管道看板          | 前端 | 2-3 天      | ⭐⭐⭐⭐               |
| P2     | T8 guest 可评估      | 全栈 | 1 天        | ⭐⭐⭐                |
| P2     | T9 数据看板          | 全栈 | 2 天        | ⭐⭐⭐                |
| P3     | T10-T14              | 混合 | 各半天-2 天 | ⭐⭐                 |

