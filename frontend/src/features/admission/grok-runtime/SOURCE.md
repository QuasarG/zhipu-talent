# Upstream character runtime

Source: https://github.com/blessonism/grok-icon-study

Pinned revision: `647e9bd7c60290c42a738fad586589b3f36a4680`

`geometry-data.js` is from `replica/geometry-data.js`. The seven engine files are from `replica/src/`. Their source contents are unchanged apart from line ending normalization. `UPSTREAM-README.md` preserves the original disclaimer. `runtime.js` and its TypeScript declaration are local integration files.

The upstream states that its characters, trademarks, geometry and extracted content belong to xAI / the relevant rights holders and are for learning only, not commercial use or redistribution. This directory does not relicense that material. Internal use is not a determination of permission. User requested direct integration for internal evaluation; this change is not deployed or pushed by the integration task.

No application bundle extraction, Dock icons, logos, playground page or network loader is included. The runtime is bundled locally and dynamically loaded only when the collaboration avatar mounts. Original expression, eye morphing, spring and pose code is used rather than reproduced in CSS.
