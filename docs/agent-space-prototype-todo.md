# Agent 协作空间原型 TODO

## 目标与边界

让用户直接看懂：谁正在工作、谁向谁交接、传了什么、各 Agent 如何执行任务。

已确认方向：抽象几何 Agent，靠近、转向与交接体现协作；不做写实办公室或具体人物。详情复用已有问答 Agent 的流式消息和工具组件。

- 仅制作独立交互原型，不改生产评估页面、不部署、不写真实业务数据。
- 示例事件、材料、输出和播放时间全部标注为合成演示，不冒充真实运行或模型 token 流。
- 不控制浏览器；构建与静态检查由开发完成，视觉及交互体验由用户验收。
- 参考 grok-icon-study 的状态表达思路，不复制其角色、几何数据、提取素材或代码；该仓库声明仅供学习、禁止商用与再分发。
- 分支：`codex/agent-space-prototype`。原型不合并到 master；确认设计后再制定生产接入任务。

## 勾选规则

完成一项，立即更新该项勾选与必要说明。写了代码不等于通过验证；实现、验证、用户验收分别勾选。未完成或仅开始的项保持未勾选。

## 1. 参考与准备

- [x] 确认抽象角色、空间交互、工作详情的设计方向。
- [x] 检查参考仓库说明与素材使用限制。
- [x] 检查现有问答 `AssistantMessage`、`ToolCallCard`、分段事件更新逻辑。
- [x] 确认两条实际链路：评审团逐个执行；准入任务并行评分，系统不算 Agent。
- [x] 建立独立原型分支，保留生产分支不变。
- [x] 建立本 TODO 文档，作为后续执行和验收清单。

## 2. 演示事件与状态

- [x] 编写合成场景脚本初稿：评审团与准入评估两组数据。
- [x] 编写按播放时间恢复 Agent 状态与独立消息的函数初稿。
- [x] 核对评审团场景：动态加入、五类评审员、串行执行、回传与原实例续派。
- [x] 核对准入场景：能力映射、独立并行评分、总审与系统交接。
- [x] 核对失败与恢复：工具失败明确可见，后续替代操作有对应事件。
- [x] 核对消息隔离：不同 Agent、不同任务及轮次的输出不会串流。
- [x] 核对播放结束、暂停、重新播放与定位历史时的状态一致性。

## 3. 原型入口

- [x] 完成独立开发入口 `/prototype/agent-space`，无需生产登录或业务接口。
- [x] 完成单命令启动：在 `frontend` 中运行 `npm run prototype:agents`。
- [x] 确保入口仅开发环境启用，生产构建不包含原型入口或演示组件。（`main.tsx` 以 `import.meta.env.DEV && pathname` 门控；生产构建产物 grep `asp-root`/`协作现场`/`prototype` 等特征零命中。）
- [x] 页面固定显示“交互原型 / 合成演示”，并解释两种场景的调度区别。

当前进度：入口、页面与启动命令完成；构建与 HTTP 可达性已验证，等待用户视觉验收。

## 4. 原创 Agent 与协作动作

- [x] 制作原创几何形体，用名字与辅助颜色识别身份，不依赖颜色区分角色。
- [x] 表达等待、工作、接收信息、交接、完成等状态；同时提供文字说明。
- [x] 实现事件触发的靠近、交接、归位，不随机游走或虚构交流。
- [x] 展示可点击的交接摘要，明确发送方、接收方和信息内容。
- [x] 实现实例动态加入与续派身份保留，不固定摆满未执行的 Agent。
- [x] 保持用户选中的详情稳定，不被新事件抢走；另提供主动跟随开关。
- [x] 支持减少动效；动画不阻塞消息更新，不延迟真实状态表达。

## 5. 流式详情与材料

- [x] 复用已有问答消息渲染，展示增量 Markdown 与按顺序出现的工具操作。
- [x] 展示工具运行中、成功、失败以及可展开的输入和返回内容。
- [x] 展示所选 Agent 收到的任务、当前轮次、工作产出和交接记录。
- [x] 空间只保留简短工作摘要，长内容在详情中阅读。
- [x] 演示材料在本地查看或明确标注，不生成指向真实候选人的虚假引用。
- [x] 跟随最新内容可暂停，阅读历史时不强制滚回底部。

## 6. 布局与操作

- [x] A：空间优先——中央协作空间，右侧所选 Agent 详情。
- [x] B：内容优先——消息阅读为主，紧凑空间保留全局协作状态。
- [x] C：交接优先——聚焦当前交流双方与信息内容，保留团队上下文。
- [x] 三种布局共用场景与状态，可通过 `?variant=` 和布局切换条切换。
- [x] 提供播放、暂停、重播、速度与回看定位控件。
- [x] 提供鼠标与键盘可操作的 Agent 选择、交接查看及布局切换。
- [x] 窄屏下改为上下布局，避免角色与详情互相遮挡。（响应式规则已实现，实际效果待用户验收。）

## 7. 开发验证与交付

- [x] TypeScript 与生产构建通过；记录警告，不把构建成功当作视觉验收。（构建通过；既有警告 2 条：`TalentChat.tsx` 动静态双导入、index chunk 超 500 kB，均与原型无关、未在本分支处理。）
- [x] 检查生产构建未带入原型内容。（对 dist 产物全量 grep 原型特征字符串，零命中；死代码消除移除了 DEV-only 动态导入。）
- [x] 运行一次设计静态检查，修正相关机械问题并记录限制。（oxlint 0 error / 12 warning，警告全部位于生产代码文件，原型文件零警告；生产警告超出原型范围，仅记录。）
- [x] 启动本地原型，使用 HTTP 检查入口和模块可达，不控制浏览器。（入口与页面/场景/CSS/main 模块均返回 200，且返回内容包含最新编辑。）
- [x] 更新原型说明：访问地址、操作方法、复用组件、合成数据及未接入能力。（见下方“原型使用说明”。）
- [x] 核对 Git 差异与本清单，仅在试验分支保存原型；不部署、不合并 master。（差异仅 `package.json`、`main.tsx`、原型目录、本清单与原型页面，均在 `codex/agent-space-prototype` 分支。）
- [x] 向用户提供可访问地址，并明确未进行浏览器视觉验证。（地址见“原型使用说明”；开发未做浏览器视觉验收。）

## 7.5 原型使用说明

- 访问地址：`cd frontend && npm run prototype:agents`，打开 `http://127.0.0.1:5178/prototype/agent-space`（仅开发环境注册该入口；追加 `?variant=A|B|C` 直达布局）。
- 操作方法：底部播放条播放/暂停/重播/调速/拖动定位；点击 Agent 固定详情，“已跟随当前 Agent”切回自动跟随；点击交接条或交接记录回看该次交接；左右方向键或右下角胶囊切换 A/B/C 布局；“减少动效”冻结姿态动画。
- 复用组件：`AssistantMessage`（问答消息与工具卡片渲染）、`Icon`、主题与 i18n Provider；状态由 `scenarios.ts` 的 `snapshot()` 按播放时间重建。
- 合成数据：两个场景的全部事件、材料、文字输出均为预编写演示；不调用模型、不读取真实候选人、不产生评分。
- 未接入能力：真实事件流、评估服务、鉴权、部署、生产路由均未接入；接入需另行规划。
- 参考边界：仅借鉴 grok-icon-study 的“状态机驱动姿态 + 低频小幅保持活性 + 状态切换瞬时弹跳”思路；未复制其角色、几何数据、素材或代码。

## 8. 用户验收（不得由开发代勾）

- [ ] 能一眼识别当前工作的 Agent，而不需要读完整日志。
- [ ] 靠近与交接动作有协作感，且不会干扰阅读。
- [ ] 能查清一次交接传递的具体内容，并追溯到工作过程。
- [ ] 并行执行、串行执行与续派在界面上能够区分。
- [ ] 确定采用哪一种布局或哪些组合，确认角色与动作方向。
- [ ] 用户确认后，另行规划真实事件流接入与生产实现。

## 执行记录

- 2026-09-07：用户提供 grok-icon-study 仓库地址，确认其素材归 xAI、禁止商用再分发；仅借鉴“状态机驱动姿态、低频小幅保持活性、状态切换瞬时弹跳、状态化眼神”思路，原创实现原型的姿态系统：空闲呼吸、周期眨眼、工作中专注下视（保留）、收到信息弹跳睁眼、完成小跳，均受“减少动效”与暂停冻结控制。未复制其角色、几何数据、素材或代码。

- 2026-09-07：完成第 7 节开发验证与交付：生产构建通过（既有警告 2 条，与原型无关）；dist 产物不含原型内容；oxlint 原型文件零警告；本地原型入口与模块 HTTP 200 可达且为最新代码；Git 差异仅原型四项，全部位于试验分支。第 8 节用户验收待用户进行。

- 2026-09-07：完成页面初版及三种布局，复用原有问答渲染器；消息按实例与轮次隔离。逐 50ms 检查合成场景快照，串行、并行、回看重建、结束状态与续派轮次检查通过。进入构建验证阶段。

- 2026-09-07：用户要求先写文档清单，已暂停后续界面实现并建立本文档。现有代码仅包括演示数据、状态函数初稿和未完成的开发入口；尚未启动、构建验证或部署原型。
